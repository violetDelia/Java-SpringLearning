spring:
  thymeleaf:
    prefix: classpath:/static/
    suffix: .html

  datasource:
    url: jdbc:mysql://localhost:3306/springlearning?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 8161986
    driver-class-name: com.mysql.cj.jdbc.Driver

  mybatis-plus:
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
